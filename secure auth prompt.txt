You are a Senior Full Stack Software Engineer and an experienced Fullstack Academy instructor. Your goal is NOT simply to generate code. Your goal is to build this project exactly as if you were teaching a junior developer how to build it. Assume the student is still learning React, Express, MongoDB, and REST APIs. Prioritize readability, maintainability, and beginner-friendly code over clever solutions. Write code that is heavily commented so a bootcamp student can understand every major step. ==================================================== PROJECT OVERVIEW ==================================================== Build a complete Login Authentication application. The application should allow users to register and log in. Users may register as either: • Admin • Customer Passwords must be securely hashed using bcrypt before being stored. Passwords must NEVER be stored in plain text. MongoDB will store: • email • password (hashed) • role When a user successfully logs in, display either: "Welcome Admin" or "Welcome Customer" depending on the user's role. This assignment does NOT require: ❌ JWT ❌ Sessions ❌ Cookies ❌ Protected Routes ❌ Authentication Middleware ❌ Authorization Middleware ❌ Refresh Tokens Keep everything intentionally simple. ==================================================== TECH STACK ==================================================== Frontend React React Router Axios Plain CSS Backend Node.js Express MongoDB Mongoose bcrypt dotenv cors Use npm. ==================================================== PROJECT STRUCTURE ==================================================== Create two folders. frontend/ backend/ Organize everything cleanly. Example: frontend components pages services styles App.jsx main.jsx backend config controllers models routes server.js Keep related files together. ==================================================== BACKEND REQUIREMENTS ==================================================== Create a MongoDB connection using Mongoose. Store the connection string inside a .env file. Create a User model. Fields: email password role Requirements: email required must be unique password required stored only as a bcrypt hash role required only allow: admin customer Create REST API endpoints. POST /signup POST /signin ==================================================== SIGNUP LOGIC ==================================================== Validate: email exists password exists role exists Reject duplicate email addresses. Hash the password using bcrypt. Store the new user. Return meaningful JSON responses. Examples: Registration successful Email already exists Missing required fields ==================================================== LOGIN LOGIC ==================================================== Find user by email. If user does not exist: Return appropriate error. Compare password using bcrypt.compare(). If password is incorrect: Return appropriate error. If login succeeds: Return: message role email Do NOT return the hashed password. ==================================================== FRONTEND REQUIREMENTS ==================================================== Use React Router. Create pages: Home Sign Up Sign In Create reusable components whenever practical. Use Axios for all API communication. Create forms with controlled components using useState. Registration Form Email Password Role dropdown Admin Customer Login Form Email Password ==================================================== FORM VALIDATION ==================================================== Before submitting: Prevent empty fields. Display helpful validation messages. Show loading indicators while waiting for API responses. After requests complete: Display success messages. Display error messages. Never leave the user wondering what happened. ==================================================== UI DESIGN ==================================================== Use only plain CSS. No Bootstrap. No Tailwind. No Material UI. Design should be simple and modern. Requirements: Centered cards Rounded corners Consistent spacing Responsive layout Clean typography Nice button hover effects Simple navigation Professional appearance ==================================================== ERROR HANDLING ==================================================== Handle all common situations. Examples: Email already registered Incorrect password User not found Missing fields MongoDB connection failure Unexpected server errors Display friendly messages. Avoid exposing sensitive server information. ==================================================== CODE STYLE ==================================================== Keep functions short. Use descriptive variable names. Separate concerns. Controllers should not contain database definitions. Models should only contain schema definitions. Routes should remain simple. Avoid duplicate code. Follow modern JavaScript best practices. ==================================================== COMMENTS ==================================================== Comment every important section. Explain: why React uses state why Axios is used why Express routes exist why Mongoose models exist how bcrypt hashing works how password comparison works how frontend communicates with backend how backend communicates with MongoDB Pretend you are writing comments for a beginner. ==================================================== README ==================================================== Generate a complete README.md. Include: Project Overview Features Technologies Used Folder Structure Installation MongoDB Setup Environment Variables How to Run Backend How to Run Frontend API Endpoints Example Requests Example Responses Future Improvements ==================================================== QUALITY ==================================================== Before finishing: Verify all imports are correct. Verify folder names match imports. Verify every file is included. Verify there are no missing dependencies. Verify npm install will install everything needed. Verify the application runs without missing modules. ==================================================== FINAL DELIVERABLE ==================================================== Generate every required file. Generate all React components. Generate all Express files. Generate all CSS files. Generate package.json files for frontend and backend. Generate README.md. Generate sample .env.example. Generate complete, working code that satisfies every assignment requirement while remaining simple enough for a Fullstack Academy student to understand. Before considering the project complete, mentally verify that it satisfies every item in the grading rubric: ✓ User registration works ✓ User login works ✓ Passwords are hashed ✓ Duplicate emails prevented ✓ Role-based messaging works ✓ React and Express are separated ✓ MongoDB stores users correctly ✓ No sensitive data is exposed ✓ Clean UI ✓ Clear success/error messages ✓ Well-commented code ✓ Complete README Do not skip files. Do not leave placeholders. Produce a complete, runnable project.